{
  "name": "diamond-smart-contracts",
  "version": "1.0.0",
  "description": "RAIR Smart Contracts v2",
  "main": "index.js",
  "type": "module",
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.7",
    "@openzeppelin/contracts": "^5.0.2",
    "@solidstate/contracts": "^1.0.0-next.3",
    "@solidstate/hardhat-contract-sizer": "^3.0.0",
    "dotenv": "^17.4.2",
    "hardhat": "^3.9.0",
    "hardhat-deploy": "^2.0.8"
  },
  "scripts": {
    "build": "npx hardhat build && node scripts/aggregateAbis.js",
    "compile": "npx hardhat build && node scripts/aggregateAbis.js",
    "test": "npx hardhat test",
    "deploy-sepolia": "npx hardhat --network 0xaa36a7 deploy",
    "deploy-amoy": "npx hardhat --network 0x13882 deploy",
    "deploy-minato": "npx hardhat --network 0x79a deploy",
    "PAYABLE-deploy-base": "npx hardhat --network 0x2105 deploy",
    "PAYABLE-deploy-astar": "npx hardhat --network 0x250 deploy",
    "PAYABLE-deploy-matic": "npx hardhat --network 0x89 deploy",
    "PAYABLE-deploy-ethereum": "npx hardhat --network 0x1 deploy",
    "PAYABLE-deploy-core": "npx hardhat --network 0x45c deploy"
  },
  "author": "Juan Miguel Sanchez Mola",
  "license": "ISC"
}
